# interactive_kernel_build

1. Run `./interactive_kernel_build/kernel_build.sh`, answer the prompts, and complete the menuconfig step when it pops up

2. After the run, inspect the reported log file to confirm each command’s stdout/stderr was captured.

3. To remove a kernel version, run `./interactive_kernel_build/kernel_remove.sh` and follow the prompts.
